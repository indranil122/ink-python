export const pyodideVersion = "314.0.7";

export const pyodideIndexUrl = `https://cdn.jsdelivr.net/pyodide/v${pyodideVersion}/full/`;

export const pyodideModuleUrl = `${pyodideIndexUrl}pyodide.mjs`;

export const executionTimeoutMs = 5000;

export const loadTimeoutMs = 60000;
